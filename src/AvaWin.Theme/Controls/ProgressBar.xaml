<ResourceDictionary xmlns="https://github.com/avaloniaui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:converters="using:Avalonia.Controls.Converters"
                    x:ClassModifier="internal">
  <Design.PreviewWith>
    <Border Padding="20">
      <StackPanel Spacing="12">
        <ProgressBar Value="50" />
        <ProgressBar Value="50" Classes="medium" />
        <ProgressBar Value="50" Classes="large" />
        <ProgressBar IsIndeterminate="True" />
        <ProgressBar Value="50" Classes="paused" />
        <ProgressBar Value="50" Classes="error" />
        <ProgressBar Value="50" IsEnabled="False" />
        <StackPanel Orientation="Horizontal" Spacing="12">
          <ProgressBar IsIndeterminate="True" Classes="ring" />
          <ProgressBar IsIndeterminate="True" Classes="ring medium" />
          <ProgressBar IsIndeterminate="True" Classes="ring large" />
        </StackPanel>
      </StackPanel>
    </Border>
  </Design.PreviewWith>

  <converters:StringFormatConverter x:Key="StringFormatConverter" />

  <ControlTheme x:Key="{x:Type ProgressBar}" TargetType="ProgressBar">
    <Setter Property="Foreground" Value="{DynamicResource ProgressBarForeground}" />
    <Setter Property="Background" Value="{DynamicResource ProgressBarBackground}" />
    <Setter Property="BorderThickness" Value="{DynamicResource ProgressBarBorderThemeThickness}" />
    <Setter Property="CornerRadius" Value="{DynamicResource ControlCornerRadius}" />
    <Setter Property="BorderBrush" Value="{DynamicResource ProgressBarBorderBrush}" />
    <Setter Property="MinHeight" Value="{DynamicResource ProgressBarThemeMinHeight}" />
    <Setter Property="VerticalAlignment" Value="Center" />
    <Setter Property="HorizontalAlignment" Value="Left" />
    <Setter Property="Template">
      <ControlTemplate TargetType="ProgressBar">
        <Border x:Name="ProgressBarRoot"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                ClipToBounds="True"
                CornerRadius="{TemplateBinding CornerRadius}">
          <Panel>
            <Panel x:Name="DeterminateRoot" Opacity="1">
              <Panel.Transitions>
                <Transitions>
                  <DoubleTransition Property="Opacity" Duration="0:0:0.197" />
                </Transitions>
              </Panel.Transitions>
              <Border x:Name="PART_Indicator"
                      Margin="{TemplateBinding Padding}"
                      Background="{TemplateBinding Foreground}"
                      CornerRadius="{TemplateBinding CornerRadius}" />
            </Panel>
            <Panel x:Name="IndeterminateRoot" Opacity="0">
              <Panel.Transitions>
                <Transitions>
                  <DoubleTransition Property="Opacity" Duration="0:0:0.197" />
                </Transitions>
              </Panel.Transitions>
              <Border x:Name="IndeterminateProgressBarIndicator"
                      Margin="{TemplateBinding Padding}"
                      Background="{DynamicResource ProgressBarIndeterminateForeground}"
                      CornerRadius="{TemplateBinding CornerRadius}" />
              <Border x:Name="IndeterminateProgressBarIndicator2"
                      Margin="{TemplateBinding Padding}"
                      Background="{DynamicResource ProgressBarIndeterminateForeground}"
                      CornerRadius="{TemplateBinding CornerRadius}" />
            </Panel>
            <LayoutTransformControl x:Name="PART_LayoutTransformControl"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    IsVisible="{TemplateBinding ShowProgressText}">
              <TextBlock Foreground="{DynamicResource ProgressBarTextForeground}" FontSize="{DynamicResource WinFontSizeXXSmall}">
                <TextBlock.Text>
                  <MultiBinding Converter="{StaticResource StringFormatConverter}">
                    <TemplateBinding Property="ProgressTextFormat" />
                    <Binding Path="Value" RelativeSource="{RelativeSource TemplatedParent}" />
                    <TemplateBinding Property="Percentage" />
                    <TemplateBinding Property="Minimum" />
                    <TemplateBinding Property="Maximum" />
                  </MultiBinding>
                </TextBlock.Text>
              </TextBlock>
            </LayoutTransformControl>
          </Panel>
        </Border>
      </ControlTemplate>
    </Setter>

    <Style Selector="^:horizontal /template/ Border#PART_Indicator">
      <Setter Property="HorizontalAlignment" Value="Left" />
      <Setter Property="VerticalAlignment" Value="Stretch" />
    </Style>
    <Style Selector="^:vertical /template/ Border#PART_Indicator">
      <Setter Property="HorizontalAlignment" Value="Stretch" />
      <Setter Property="VerticalAlignment" Value="Bottom" />
    </Style>
    <Style Selector="^:horizontal">
      <Setter Property="Width" Value="{DynamicResource ProgressBarWidth}" />
      <Setter Property="Height" Value="{DynamicResource ProgressBarHeight}" />
    </Style>
    <Style Selector="^:vertical">
      <Setter Property="Width" Value="{DynamicResource ProgressBarHeight}" />
      <Setter Property="Height" Value="{DynamicResource ProgressBarWidth}" />
    </Style>
    <Style Selector="^.medium:horizontal">
      <Setter Property="Width" Value="{DynamicResource ProgressBarWidthMedium}" />
    </Style>
    <Style Selector="^.medium:vertical">
      <Setter Property="Height" Value="{DynamicResource ProgressBarWidthMedium}" />
    </Style>
    <Style Selector="^.large:horizontal">
      <Setter Property="Width" Value="NaN" />
      <Setter Property="HorizontalAlignment" Value="Stretch" />
    </Style>
    <Style Selector="^.large:vertical">
      <Setter Property="Height" Value="NaN" />
      <Setter Property="VerticalAlignment" Value="Stretch" />
    </Style>
    <Style Selector="^:vertical /template/ LayoutTransformControl#PART_LayoutTransformControl">
      <Setter Property="LayoutTransform">
        <Setter.Value>
          <RotateTransform Angle="90" />
        </Setter.Value>
      </Setter>
    </Style>

    <Style Selector="^:indeterminate">
      <Setter Property="Background" Value="{DynamicResource ProgressBarIndeterminateBackground}" />
      <Setter Property="Padding" Value="0,1" />
    </Style>
    <Style Selector="^:indeterminate /template/ Panel#DeterminateRoot">
      <Setter Property="Opacity" Value="0" />
    </Style>
    <Style Selector="^:indeterminate /template/ Panel#IndeterminateRoot">
      <Setter Property="Opacity" Value="1" />
    </Style>
    <Style Selector="^:horizontal:indeterminate /template/ Border#IndeterminateProgressBarIndicator">
      <Style.Animations>
        <Animation IterationCount="Infinite" Duration="0:0:2">
          <KeyFrame KeySpline="0.1,0.9,0.2,1" KeyTime="0:0:0">
            <Setter Property="TranslateTransform.X" Value="{Binding $parent[ProgressBar].TemplateSettings.ContainerAnimationStartPosition}" />
          </KeyFrame>
          <KeyFrame KeySpline="0.1,0.9,0.2,1" KeyTime="0:0:1.5">
            <Setter Property="TranslateTransform.X" Value="{Binding $parent[ProgressBar].TemplateSettings.ContainerAnimationEndPosition}" />
          </KeyFrame>
          <KeyFrame KeySpline="0.1,0.9,0.2,1" KeyTime="0:0:2">
            <Setter Property="TranslateTransform.X" Value="{Binding $parent[ProgressBar].TemplateSettings.ContainerAnimationEndPosition}" />
          </KeyFrame>
        </Animation>
      </Style.Animations>
    </Style>
    <Style Selector="^:horizontal:indeterminate /template/ Border#IndeterminateProgressBarIndicator2">
      <Style.Animations>
        <Animation IterationCount="Infinite" Duration="0:0:2">
          <KeyFrame KeySpline="0.1,0.9,0.2,1" KeyTime="0:0:0">
            <Setter Property="TranslateTransform.X" Value="{Binding $parent[ProgressBar].TemplateSettings.Container2AnimationStartPosition}" />
          </KeyFrame>
          <KeyFrame KeySpline="0.1,0.9,0.2,1" KeyTime="0:0:0.75">
            <Setter Property="TranslateTransform.X" Value="{Binding $parent[ProgressBar].TemplateSettings.Container2AnimationStartPosition}" />
          </KeyFrame>
          <KeyFrame KeySpline="0.1,0.9,0.2,1" KeyTime="0:0:2">
            <Setter Property="TranslateTransform.X" Value="{Binding $parent[ProgressBar].TemplateSettings.Container2AnimationEndPosition}" />
          </KeyFrame>
        </Animation>
      </Style.Animations>
    </Style>
    <Style Selector="^:vertical:indeterminate /template/ Border#IndeterminateProgressBarIndicator">
      <Style.Animations>
        <Animation IterationCount="Infinite" Duration="0:0:2">
          <KeyFrame KeySpline="0.1,0.9,0.2,1" KeyTime="0:0:0">
            <Setter Property="TranslateTransform.Y" Value="{Binding $parent[ProgressBar].TemplateSettings.ContainerAnimationStartPosition}" />
          </KeyFrame>
          <KeyFrame KeySpline="0.1,0.9,0.2,1" KeyTime="0:0:1.5">
            <Setter Property="TranslateTransform.Y" Value="{Binding $parent[ProgressBar].TemplateSettings.ContainerAnimationEndPosition}" />
          </KeyFrame>
          <KeyFrame KeySpline="0.1,0.9,0.2,1" KeyTime="0:0:2">
            <Setter Property="TranslateTransform.Y" Value="{Binding $parent[ProgressBar].TemplateSettings.ContainerAnimationEndPosition}" />
          </KeyFrame>
        </Animation>
      </Style.Animations>
    </Style>
    <Style Selector="^:vertical:indeterminate /template/ Border#IndeterminateProgressBarIndicator2">
      <Style.Animations>
        <Animation IterationCount="Infinite" Duration="0:0:2">
          <KeyFrame KeySpline="0.1,0.9,0.2,1" KeyTime="0:0:0">
            <Setter Property="TranslateTransform.Y" Value="{Binding $parent[ProgressBar].TemplateSettings.Container2AnimationStartPosition}" />
          </KeyFrame>
          <KeyFrame KeySpline="0.1,0.9,0.2,1" KeyTime="0:0:0.75">
            <Setter Property="TranslateTransform.Y" Value="{Binding $parent[ProgressBar].TemplateSettings.Container2AnimationStartPosition}" />
          </KeyFrame>
          <KeyFrame KeySpline="0.1,0.9,0.2,1" KeyTime="0:0:2">
            <Setter Property="TranslateTransform.Y" Value="{Binding $parent[ProgressBar].TemplateSettings.Container2AnimationEndPosition}" />
          </KeyFrame>
        </Animation>
      </Style.Animations>
    </Style>
    <Style Selector="^:horizontal /template/ Border#IndeterminateProgressBarIndicator">
      <Setter Property="Width" Value="{Binding $parent[ProgressBar].TemplateSettings.ContainerWidth}" />
    </Style>
    <Style Selector="^:horizontal /template/ Border#IndeterminateProgressBarIndicator2">
      <Setter Property="Width" Value="{Binding $parent[ProgressBar].TemplateSettings.Container2Width}" />
    </Style>
    <Style Selector="^:vertical /template/ Border#IndeterminateProgressBarIndicator">
      <Setter Property="Height" Value="{Binding $parent[ProgressBar].TemplateSettings.ContainerWidth}" />
    </Style>
    <Style Selector="^:vertical /template/ Border#IndeterminateProgressBarIndicator2">
      <Setter Property="Height" Value="{Binding $parent[ProgressBar].TemplateSettings.Container2Width}" />
    </Style>

    <Style Selector="^.paused">
      <Setter Property="Opacity" Value="0.5" />
      <Setter Property="Transitions">
        <Transitions>
          <DoubleTransition Property="Opacity" Duration="0:0:3" Easing="0.03,0.76,0.31,1" />
        </Transitions>
      </Setter>
    </Style>
    <Style Selector="^.error /template/ Border#PART_Indicator">
      <Setter Property="IsVisible" Value="False" />
    </Style>
    <Style Selector="^:disabled">
      <Setter Property="Foreground" Value="{DynamicResource ProgressBarForegroundDisabled}" />
    </Style>

    <Style Selector="^.ring">
      <Setter Property="Width" Value="{DynamicResource ProgressRingSize}" />
      <Setter Property="Height" Value="{DynamicResource ProgressRingSize}" />
      <Setter Property="MinHeight" Value="0" />
      <Setter Property="Background" Value="{DynamicResource ProgressBarIndeterminateBackground}" />
      <Setter Property="Template">
        <ControlTemplate TargetType="ProgressBar">
          <Panel x:Name="ProgressBarRoot" Background="{TemplateBinding Background}">
            <Border x:Name="PART_Indicator" IsVisible="False" />
            <Panel x:Name="Dot0" Classes="dot"><Ellipse Classes="dot" Fill="{TemplateBinding Foreground}" /></Panel>
            <Panel x:Name="Dot1" Classes="dot"><Ellipse Classes="dot" Fill="{TemplateBinding Foreground}" /></Panel>
            <Panel x:Name="Dot2" Classes="dot"><Ellipse Classes="dot" Fill="{TemplateBinding Foreground}" /></Panel>
            <Panel x:Name="Dot3" Classes="dot"><Ellipse Classes="dot" Fill="{TemplateBinding Foreground}" /></Panel>
            <Panel x:Name="Dot4" Classes="dot"><Ellipse Classes="dot" Fill="{TemplateBinding Foreground}" /></Panel>
          </Panel>
        </ControlTemplate>
      </Setter>
      <Style Selector="^ /template/ Panel.dot">
        <Setter Property="RenderTransformOrigin" Value="50%,50%" />
        <Setter Property="RenderTransform">
          <Setter.Value>
            <RotateTransform Angle="0" />
          </Setter.Value>
        </Setter>
        <Setter Property="Opacity" Value="0" />
      </Style>
      <Style Selector="^ /template/ Ellipse.dot">
        <Setter Property="Width" Value="{DynamicResource ProgressRingDotSize}" />
        <Setter Property="Height" Value="{DynamicResource ProgressRingDotSize}" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Top" />
      </Style>
      <Style Selector="^:indeterminate /template/ Panel.dot">
        <Setter Property="Opacity" Value="1" />
      </Style>
      <Style Selector="^:indeterminate /template/ Panel#Dot0">
        <Style.Animations>
          <Animation IterationCount="Infinite" Duration="0:0:1.5" Easing="0.25,0.1,0.75,0.9">
            <KeyFrame Cue="0%"><Setter Property="RotateTransform.Angle" Value="0" /></KeyFrame>
            <KeyFrame Cue="100%"><Setter Property="RotateTransform.Angle" Value="360" /></KeyFrame>
          </Animation>
        </Style.Animations>
      </Style>
      <Style Selector="^:indeterminate /template/ Panel#Dot1">
        <Style.Animations>
          <Animation IterationCount="Infinite" Duration="0:0:1.5" Delay="0:0:0.167" Easing="0.25,0.1,0.75,0.9">
            <KeyFrame Cue="0%"><Setter Property="RotateTransform.Angle" Value="0" /></KeyFrame>
            <KeyFrame Cue="100%"><Setter Property="RotateTransform.Angle" Value="360" /></KeyFrame>
          </Animation>
        </Style.Animations>
      </Style>
      <Style Selector="^:indeterminate /template/ Panel#Dot2">
        <Style.Animations>
          <Animation IterationCount="Infinite" Duration="0:0:1.5" Delay="0:0:0.334" Easing="0.25,0.1,0.75,0.9">
            <KeyFrame Cue="0%"><Setter Property="RotateTransform.Angle" Value="0" /></KeyFrame>
            <KeyFrame Cue="100%"><Setter Property="RotateTransform.Angle" Value="360" /></KeyFrame>
          </Animation>
        </Style.Animations>
      </Style>
      <Style Selector="^:indeterminate /template/ Panel#Dot3">
        <Style.Animations>
          <Animation IterationCount="Infinite" Duration="0:0:1.5" Delay="0:0:0.501" Easing="0.25,0.1,0.75,0.9">
            <KeyFrame Cue="0%"><Setter Property="RotateTransform.Angle" Value="0" /></KeyFrame>
            <KeyFrame Cue="100%"><Setter Property="RotateTransform.Angle" Value="360" /></KeyFrame>
          </Animation>
        </Style.Animations>
      </Style>
      <Style Selector="^:indeterminate /template/ Panel#Dot4">
        <Style.Animations>
          <Animation IterationCount="Infinite" Duration="0:0:1.5" Delay="0:0:0.668" Easing="0.25,0.1,0.75,0.9">
            <KeyFrame Cue="0%"><Setter Property="RotateTransform.Angle" Value="0" /></KeyFrame>
            <KeyFrame Cue="100%"><Setter Property="RotateTransform.Angle" Value="360" /></KeyFrame>
          </Animation>
        </Style.Animations>
      </Style>
    </Style>
    <Style Selector="^.ring.medium">
      <Setter Property="Width" Value="{DynamicResource ProgressRingSizeMedium}" />
      <Setter Property="Height" Value="{DynamicResource ProgressRingSizeMedium}" />
      <Style Selector="^ /template/ Ellipse.dot">
        <Setter Property="Width" Value="{DynamicResource ProgressRingDotSizeMedium}" />
        <Setter Property="Height" Value="{DynamicResource ProgressRingDotSizeMedium}" />
      </Style>
    </Style>
    <Style Selector="^.ring.large">
      <Setter Property="Width" Value="{DynamicResource ProgressRingSizeLarge}" />
      <Setter Property="Height" Value="{DynamicResource ProgressRingSizeLarge}" />
      <Setter Property="HorizontalAlignment" Value="Left" />
      <Setter Property="VerticalAlignment" Value="Center" />
      <Style Selector="^ /template/ Ellipse.dot">
        <Setter Property="Width" Value="{DynamicResource ProgressRingDotSizeLarge}" />
        <Setter Property="Height" Value="{DynamicResource ProgressRingDotSizeLarge}" />
      </Style>
    </Style>
  </ControlTheme>
</ResourceDictionary>

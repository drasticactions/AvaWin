<ResourceDictionary xmlns="https://github.com/avaloniaui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    x:ClassModifier="internal">
  <Design.PreviewWith>
    <Border Padding="20" Width="200" Height="200">
      <ScrollViewer>
        <StackPanel Width="400" Height="600">
          <TextBlock Text="Scroll me" />
        </StackPanel>
      </ScrollViewer>
    </Border>
  </Design.PreviewWith>

  <MenuFlyout x:Key="VerticalScrollBarContextFlyout">
    <MenuItem Header="{DynamicResource StringScrollBarScrollHere}" Command="{Binding $parent[ScrollBar].ScrollHere}" />
    <Separator />
    <MenuItem Header="{DynamicResource StringScrollBarTop}" Command="{Binding $parent[ScrollBar].ScrollToHome}" />
    <MenuItem Header="{DynamicResource StringScrollBarBottom}" Command="{Binding $parent[ScrollBar].ScrollToEnd}" />
    <Separator />
    <MenuItem Header="{DynamicResource StringScrollBarPageUp}" Command="{Binding $parent[ScrollBar].PageUp}" />
    <MenuItem Header="{DynamicResource StringScrollBarPageDown}" Command="{Binding $parent[ScrollBar].PageDown}" />
    <Separator />
    <MenuItem Header="{DynamicResource StringScrollBarScrollUp}" Command="{Binding $parent[ScrollBar].LineUp}" />
    <MenuItem Header="{DynamicResource StringScrollBarScrollDown}" Command="{Binding $parent[ScrollBar].LineDown}" />
  </MenuFlyout>
  <MenuFlyout x:Key="HorizontalScrollBarContextFlyout">
    <MenuItem Header="{DynamicResource StringScrollBarScrollHere}" Command="{Binding $parent[ScrollBar].ScrollHere}" />
    <Separator />
    <MenuItem Header="{DynamicResource StringScrollBarLeftEdge}" Command="{Binding $parent[ScrollBar].ScrollToHome}" />
    <MenuItem Header="{DynamicResource StringScrollBarRightEdge}" Command="{Binding $parent[ScrollBar].ScrollToEnd}" />
    <Separator />
    <MenuItem Header="{DynamicResource StringScrollBarPageLeft}" Command="{Binding $parent[ScrollBar].PageLeft}" />
    <MenuItem Header="{DynamicResource StringScrollBarPageRight}" Command="{Binding $parent[ScrollBar].PageRight}" />
    <Separator />
    <MenuItem Header="{DynamicResource StringScrollBarScrollLeft}" Command="{Binding $parent[ScrollBar].LineLeft}" />
    <MenuItem Header="{DynamicResource StringScrollBarScrollRight}" Command="{Binding $parent[ScrollBar].LineRight}" />
  </MenuFlyout>

  <ControlTheme x:Key="WinScrollBarThumb" TargetType="Thumb">
    <Setter Property="Background" Value="{DynamicResource ScrollBarPanningThumbBackground}" />
    <Setter Property="Template">
      <ControlTemplate>
        <Border Background="{TemplateBinding Background}" CornerRadius="{TemplateBinding CornerRadius}" />
      </ControlTemplate>
    </Setter>
    <Setter Property="Transitions">
      <Transitions>
        <TransformOperationsTransition Property="RenderTransform" Duration="0:0:0.1" />
      </Transitions>
    </Setter>
    <Style Selector="^:pointerover /template/ Border">
      <Setter Property="Background" Value="{DynamicResource ScrollBarThumbFillPointerOver}" />
    </Style>
    <Style Selector="^:pressed /template/ Border">
      <Setter Property="Background" Value="{DynamicResource ScrollBarThumbFillPressed}" />
    </Style>
    <Style Selector="^:disabled /template/ Border">
      <Setter Property="Background" Value="{DynamicResource ScrollBarThumbFillDisabled}" />
    </Style>
  </ControlTheme>

  <ControlTheme x:Key="WinScrollBarPageButton" TargetType="RepeatButton">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="VerticalAlignment" Value="Stretch" />
    <Setter Property="HorizontalAlignment" Value="Stretch" />
    <Setter Property="Opacity" Value="0" />
    <Setter Property="Template">
      <ControlTemplate>
        <Border Background="{TemplateBinding Background}" />
      </ControlTemplate>
    </Setter>
  </ControlTheme>

  <ControlTheme x:Key="WinScrollBarLineButton" TargetType="RepeatButton">
    <Setter Property="Opacity" Value="0" />
    <Setter Property="MinWidth" Value="0" />
    <Setter Property="MinHeight" Value="0" />
    <Setter Property="Padding" Value="0" />
    <Setter Property="FontFamily" Value="{DynamicResource WinSymbolFontFamily}" />
    <Setter Property="FontSize" Value="{DynamicResource ScrollBarButtonArrowIconFontSize}" />
    <Setter Property="TextElement.Foreground" Value="{DynamicResource ScrollBarButtonArrowForeground}" />
    <Setter Property="RenderTransform" Value="{x:Null}" />
    <Setter Property="Template">
      <ControlTemplate>
        <ContentPresenter x:Name="PART_ContentPresenter"
                          Background="{DynamicResource ScrollBarButtonBackground}"
                          BorderBrush="{DynamicResource ScrollBarButtonBorderBrush}"
                          Content="{TemplateBinding Content}"
                          HorizontalContentAlignment="Center"
                          VerticalContentAlignment="Center" />
      </ControlTemplate>
    </Setter>
    <Setter Property="Transitions">
      <Transitions>
        <DoubleTransition Property="Opacity" Duration="0:0:0.1" />
      </Transitions>
    </Setter>
    <Style Selector="^:pointerover">
      <Setter Property="TextElement.Foreground" Value="{DynamicResource ScrollBarButtonArrowForegroundPointerOver}" />
      <Style Selector="^ /template/ ContentPresenter">
        <Setter Property="Background" Value="{DynamicResource ScrollBarButtonBackgroundPointerOver}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ScrollBarButtonBorderBrushPointerOver}" />
      </Style>
    </Style>
    <Style Selector="^:pressed">
      <Setter Property="TextElement.Foreground" Value="{DynamicResource ScrollBarButtonArrowForegroundPressed}" />
      <Style Selector="^ /template/ ContentPresenter">
        <Setter Property="Background" Value="{DynamicResource ScrollBarButtonBackgroundPressed}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ScrollBarButtonBorderBrushPressed}" />
      </Style>
    </Style>
    <Style Selector="^:disabled">
      <Setter Property="TextElement.Foreground" Value="{DynamicResource ScrollBarButtonArrowForegroundDisabled}" />
      <Style Selector="^ /template/ ContentPresenter">
        <Setter Property="Background" Value="{DynamicResource ScrollBarButtonBackgroundDisabled}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ScrollBarButtonBorderBrushDisabled}" />
      </Style>
    </Style>
  </ControlTheme>

  <ControlTheme x:Key="{x:Type ScrollBar}" TargetType="ScrollBar">
    <Setter Property="MinWidth" Value="{DynamicResource ScrollBarSize}" />
    <Setter Property="MinHeight" Value="{DynamicResource ScrollBarSize}" />
    <Setter Property="Background" Value="{DynamicResource ScrollBarBackground}" />
    <Setter Property="Foreground" Value="{DynamicResource ScrollBarForeground}" />
    <Setter Property="BorderBrush" Value="{DynamicResource ScrollBarBorderBrush}" />
    <Style Selector="^:vertical">
      <Setter Property="ContextFlyout" Value="{StaticResource VerticalScrollBarContextFlyout}" />
      <Setter Property="Template">
        <ControlTemplate>
          <Grid x:Name="Root">
            <Border x:Name="VerticalRoot" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}">
              <Grid RowDefinitions="Auto,*,Auto">
                <Rectangle x:Name="TrackRect"
                           Fill="{DynamicResource ScrollBarTrackFill}"
                           Stroke="{DynamicResource ScrollBarTrackStroke}"
                           StrokeThickness="{DynamicResource ScrollBarTrackBorderThemeThickness}"
                           Opacity="0"
                           Grid.RowSpan="3">
                  <Rectangle.Transitions>
                    <Transitions>
                      <DoubleTransition Property="Opacity" Duration="0:0:0.1" />
                    </Transitions>
                  </Rectangle.Transitions>
                </Rectangle>
                <RepeatButton Name="PART_LineUpButton"
                              HorizontalAlignment="Center"
                              Theme="{StaticResource WinScrollBarLineButton}"
                              Grid.Row="0"
                              Focusable="False"
                              MinWidth="{DynamicResource ScrollBarSize}"
                              Height="{DynamicResource ScrollBarButtonSize}"
                              Width="{DynamicResource ScrollBarButtonSize}"
                              Content="&#xE0E4;"
                              AutomationProperties.Name="Line up" />
                <Track Grid.Row="1"
                       Minimum="{TemplateBinding Minimum}"
                       Maximum="{TemplateBinding Maximum}"
                       Value="{TemplateBinding Value, Mode=TwoWay}"
                       DeferThumbDrag="{TemplateBinding ScrollViewer.IsDeferredScrollingEnabled}"
                       ViewportSize="{TemplateBinding ViewportSize}"
                       Orientation="{TemplateBinding Orientation}"
                       IsDirectionReversed="True">
                  <Track.DecreaseButton>
                    <RepeatButton Name="PART_PageUpButton" Classes="largeIncrease" Theme="{StaticResource WinScrollBarPageButton}" Focusable="False" AutomationProperties.Name="Page up" />
                  </Track.DecreaseButton>
                  <Track.IncreaseButton>
                    <RepeatButton Name="PART_PageDownButton" Classes="largeIncrease" Theme="{StaticResource WinScrollBarPageButton}" Focusable="False" AutomationProperties.Name="Page down" />
                  </Track.IncreaseButton>
                  <Thumb Theme="{StaticResource WinScrollBarThumb}"
                         Width="{DynamicResource ScrollBarSize}"
                         MinHeight="{DynamicResource ScrollBarSize}"
                         RenderTransform="{DynamicResource VerticalSmallScrollThumbScaleTransform}"
                         RenderTransformOrigin="100%,50%"
                         AutomationProperties.Name="Position" />
                </Track>
                <RepeatButton Name="PART_LineDownButton"
                              HorizontalAlignment="Center"
                              Theme="{StaticResource WinScrollBarLineButton}"
                              Grid.Row="2"
                              Focusable="False"
                              MinWidth="{DynamicResource ScrollBarSize}"
                              Height="{DynamicResource ScrollBarButtonSize}"
                              Width="{DynamicResource ScrollBarButtonSize}"
                              Content="&#xE0E5;"
                              AutomationProperties.Name="Line down" />
              </Grid>
            </Border>
          </Grid>
        </ControlTemplate>
      </Setter>
    </Style>
    <Style Selector="^:horizontal">
      <Setter Property="ContextFlyout" Value="{StaticResource HorizontalScrollBarContextFlyout}" />
      <Setter Property="Template">
        <ControlTemplate>
          <Grid x:Name="Root">
            <Border x:Name="HorizontalRoot" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}">
              <Grid ColumnDefinitions="Auto,*,Auto">
                <Rectangle x:Name="TrackRect"
                           Fill="{DynamicResource ScrollBarTrackFill}"
                           Stroke="{DynamicResource ScrollBarTrackStroke}"
                           StrokeThickness="{DynamicResource ScrollBarTrackBorderThemeThickness}"
                           Opacity="0"
                           Grid.ColumnSpan="3">
                  <Rectangle.Transitions>
                    <Transitions>
                      <DoubleTransition Property="Opacity" Duration="0:0:0.1" />
                    </Transitions>
                  </Rectangle.Transitions>
                </Rectangle>
                <RepeatButton Name="PART_LineUpButton"
                              Theme="{StaticResource WinScrollBarLineButton}"
                              VerticalAlignment="Center"
                              Grid.Column="0"
                              Focusable="False"
                              MinHeight="{DynamicResource ScrollBarSize}"
                              Width="{DynamicResource ScrollBarButtonSize}"
                              Height="{DynamicResource ScrollBarButtonSize}"
                              Content="&#xE0E2;"
                              AutomationProperties.Name="Column left" />
                <Track Grid.Column="1"
                       Minimum="{TemplateBinding Minimum}"
                       Maximum="{TemplateBinding Maximum}"
                       Value="{TemplateBinding Value, Mode=TwoWay}"
                       DeferThumbDrag="{TemplateBinding ScrollViewer.IsDeferredScrollingEnabled}"
                       ViewportSize="{TemplateBinding ViewportSize}"
                       Orientation="{TemplateBinding Orientation}">
                  <Track.DecreaseButton>
                    <RepeatButton Name="PART_PageUpButton" Classes="largeIncrease" Theme="{StaticResource WinScrollBarPageButton}" Focusable="False" AutomationProperties.Name="Page left" />
                  </Track.DecreaseButton>
                  <Track.IncreaseButton>
                    <RepeatButton Name="PART_PageDownButton" Classes="largeIncrease" Theme="{StaticResource WinScrollBarPageButton}" Focusable="False" AutomationProperties.Name="Page right" />
                  </Track.IncreaseButton>
                  <Thumb Theme="{StaticResource WinScrollBarThumb}"
                         Height="{DynamicResource ScrollBarSize}"
                         MinWidth="{DynamicResource ScrollBarSize}"
                         RenderTransform="{DynamicResource HorizontalSmallScrollThumbScaleTransform}"
                         RenderTransformOrigin="50%,100%"
                         AutomationProperties.Name="Position" />
                </Track>
                <RepeatButton Name="PART_LineDownButton"
                              Theme="{StaticResource WinScrollBarLineButton}"
                              VerticalAlignment="Center"
                              Grid.Column="2"
                              Focusable="False"
                              MinHeight="{DynamicResource ScrollBarSize}"
                              Width="{DynamicResource ScrollBarButtonSize}"
                              Height="{DynamicResource ScrollBarButtonSize}"
                              Content="&#xE0E3;"
                              AutomationProperties.Name="Column right" />
              </Grid>
            </Border>
          </Grid>
        </ControlTemplate>
      </Setter>
    </Style>
    <Style Selector="^[IsExpanded=true]">
      <Style Selector="^ /template/ Grid#Root">
        <Setter Property="Background" Value="{DynamicResource ScrollBarBackgroundPointerOver}" />
      </Style>
      <Style Selector="^ /template/ Rectangle#TrackRect">
        <Setter Property="Fill" Value="{DynamicResource ScrollBarTrackFillPointerOver}" />
        <Setter Property="Stroke" Value="{DynamicResource ScrollBarTrackStrokePointerOver}" />
        <Setter Property="Opacity" Value="1" />
      </Style>
      <Style Selector="^ /template/ Thumb">
        <Setter Property="Background" Value="{DynamicResource ScrollBarThumbBackgroundColor}" />
        <Setter Property="RenderTransform" Value="none" />
      </Style>
      <Style Selector="^ /template/ RepeatButton">
        <Setter Property="Opacity" Value="1" />
      </Style>
    </Style>
  </ControlTheme>
</ResourceDictionary>
